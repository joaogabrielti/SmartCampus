import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        paddingTop: 8,
        paddingRight: 8,
        paddingLeft: 8,
    },
    info: {
        marginStart: 8,
    },
    name: {
        color: '#43a047',
        fontWeight: '700',
    },
})

export default styles